function SingleContent({id, poster, title, date, media_type, vote_average}) {
    return (
        <div>
            <div> {title} </div>
        </div>
    )
}

export default SingleContent
